name: labkesda_patient_app
description: Aplikasi Pasien Labkesda Garut — pendaftaran online, status, dan hasil lab.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Networking & storage
  http: ^1.2.1
  shared_preferences: ^2.2.3

  # UI
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
